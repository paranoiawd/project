# 이 저장소에서의 작업 지침

## 병합 권한 (사용자가 명시적으로 부여함)

**작업이 끝나면 매번 묻지 말고 `main`에 병합할 것.** 사용자가 이 권한을 명시적으로,
지속적으로 부여했다. 다음 흐름을 그대로 수행한다:

1. 지정된 작업 브랜치에서 개발하고 커밋한다
2. `git push -u origin <branch>` 로 푸시한다
3. PR을 연다 (초안으로 열었다면 초안을 해제한다)
4. **그 PR을 `main`에 병합한다** — 확인을 받으려고 멈추지 않는다
5. 로컬 `main`을 갱신하고 병합 결과를 확인한다

허락을 다시 구할 필요 없다. "머지할까요?" 라고 묻지 말고 그냥 하면 된다.

**이 권한에 포함되지 않는 것** (이건 별도로 물어볼 것):
- `main`의 히스토리를 다시 쓰는 것 (force push, rebase, reset --hard)
- 커밋이나 브랜치를 되돌리거나 삭제하는 것
- 저장소 설정 변경

**병합 전에 항상 확인할 것** — 이건 권한 문제가 아니라 품질 문제다:
- 빌드가 되는가 (`bench/build.sh`)
- C++14로 컴파일되는가 (`g++ -std=c++14 -w -I bench/shim -fsyntax-only schedular.cpp`)
- 성능 수치가 튜닝에 쓰지 않은 홀드아웃 시드에서 재현되는가

측정이 나빠졌거나 빌드가 깨졌으면 병합하지 말고 사실대로 보고한다.

## 이 프로젝트에 대해

분산병렬 과제의 로봇 태스크 스케줄러. 제출물은 `schedular.h` / `schedular.cpp` 뿐이며,
채점자는 **원본 그대로의** `main.cpp` / `simulator.{h,cpp}` 에 대고 컴파일한다.

**코드를 건드리기 전에 [`HANDOFF.md`](HANDOFF.md)를 읽을 것.** 특히:
- **§3a** — 상한 분석. 완료 목표를 받아들이기 전에 반드시 볼 것. 어떤 코드로도 넘을 수 없는
  물리적 한계가 어디인지, 그리고 실제로 우리를 구속하는 값이 무엇인지 적혀 있다
- **§6** — 측정해서 실패한 실험 55개. 여기 있는 건 다시 하지 말 것
- **§8** — 벤치 도구. 주장에는 **300시드 이상**의 근거가 필요하고, `bench/cmp.py`의
  시드별 페어 비교를 쓸 것 (평균 비교보다 해상도가 약 5배 높다).
  완료·발견 **두 지표를 항상 같이** 보고할 것 — 한쪽만 최적화했다가 이미 두 번 역효과가 났다
